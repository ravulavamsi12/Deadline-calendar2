App As appinfo:
    BackEnabled: =false
    OnStart: |+
        =Set(_Currentuser,User()); Set(_Currentdate,Today());
        ClearCollect(Tasksub,'Task subject'.'Subject Name');
        ClearCollect(categorylist,Category.CategoryName);
        ClearCollect(Deadlinesub,'Deadline Subject'.SubjectName);
        
        
        
        
        

